{
    "name": "auth-context",
    "version": "1.0.0",
    "description": "Authentication context management using async local storage",
    "main": "server.ts",
    "scripts": {
        "start": "ts-node server.ts",
        "dev": "nodemon --exec ts-node src/server.ts",
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "fastify": "^5.0.0"
    },
    "devDependencies": {
        "@types/node": "^22.7.7",
        "nodemon": "^3.1.7",
        "ts-node": "^10.9.2",
        "typescript": "^5.6.3"
    }
}
